export const USERNAME_ALREADY_EXISTS =
  'A user with the same username already exists.';
export const EMAIL_ALREADY_EXISTS = 'This email is already in use.';
export const EMAIL_OR_USER_WRONG = ' or password is not correct.';
export const REGULAR_CHECK_IS_EMAIL =
  /^([a-z0-9_-]+\.)*[a-z0-9_-]+@[a-z0-9_-]+(\.[a-z0-9_-]+)*\.[a-z]{2,6}$/;
